export const increment = (type) => {
  // {
  //  type: "INCREMENT"
  // }
  return { type };
};

export const decrement = (type) => {
  return { type };
};

export const reset = (type) => {
  return { type };
};
